[defaults]
inventory = ./inventory/hosts.toml
roles_path = ./roles
host_key_checking = False
stdout_callback = ansible.builtin.default
callback_result_format = yaml
private_key_file = ~/.ssh/id_ed25519

[inventory]
enable_plugins = toml, yaml, ini
